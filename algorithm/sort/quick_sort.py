#!/usr/bin/env python
# -*- coding: utf-8 -*-


def subSort(arr, left, right):
    # 基数，左右指针
    key = arr[0]
    l, r = left, right

    while l!=r:
        pass
    pass


def quicksort(arr, left, right):
    mid = subSort(arr,left,right)

    return []
